#include<iostream>
using namespace std;
int main(){
    cout<<"int *p,q signifies p will save an address and the variable whose address is stored will be of int type while q will save an value which will be of integer type.";
}
